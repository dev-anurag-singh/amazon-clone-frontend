import { Component } from 'react';

export class AddToCart extends Component {
  render() {
    return (
      <button className='btn btn--yellow btn--rounded btn--full'>
        Add to Cart
      </button>
    );
  }
}
